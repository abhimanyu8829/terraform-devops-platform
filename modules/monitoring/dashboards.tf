/*
Purpose: Placeholder for monitoring dashboard definitions.

How to use:
- Replace this placeholder with provider-specific dashboard resources (for example Grafana dashboards via Terraform provider or JSON dashboard files applied by CI).

What to change:
- Add concrete dashboard resources and data sources. Store dashboard JSON templates in the module and render with `templatefile` if needed.
*/

resource "null_resource" "dashboards" {}
